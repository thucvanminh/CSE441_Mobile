import {StyleSheet} from 'react-native';
let styles = {};
export default styles = StyleSheet.create({
    container: {
        backgroundColor: '#fff',
    },
    box: {
        width: 100,
        height: 100,
        justifyContent: 'center',
        alignItems: 'center',
        margin: 20
    }
    ,
});